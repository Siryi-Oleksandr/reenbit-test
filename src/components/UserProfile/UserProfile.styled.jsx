import styled from 'styled-components';

export const ProfileWrapper = styled.div`
  display: flex;
  justify-content: flex-end;
  align-items: center;
  gap: 5px;

  > img {
    width: 60px;
    height: 60px;
    border-radius: 50%;
    overflow: hidden;
  }
`;
